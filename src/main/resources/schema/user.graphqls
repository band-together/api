type User {
    id: ID!
    username: String!
    email: String!
    passwordHash: String
    name: String
}

type Query {
    getUser(id: Int!): User
}

type Mutation{
    createUser(username: String!, email: String!, password: String!, name: String!): User
}
