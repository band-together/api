type User {
    id: ID!
    username: String!
    email: String!
    name: String
}

type Query {
    getUser(id: Int!): User
}

type Mutation{
    createUser(email: String!, username: String!, password: String!, name: String!): User
}
